name: MyFirstWorkFlow
on:
  workflow_dispatch:
  schedule:
    - cron: '*/5 * * * *' 
jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
    - name: windowsone
      shell: pwsh
      run: get-computerinfo
