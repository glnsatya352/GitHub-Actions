name: githubaction
on: 
    push:
        branches: 
         - main
jobs:
    job1:
        name: job1
        runs-on: ubuntu-latest
        steps:
            - name: step1
              run: |
                  pwd
                  ls -lrt 
                  hostname
            - name: Checkout
              uses: actions/checkout@v6.0.3
            - name: step3
              run: |
               pwd
               ls -ltr
               hostname
